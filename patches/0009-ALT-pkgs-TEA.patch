From c2847ec752475fd717b0346f127dceed3df32dd0 Mon Sep 17 00:00:00 2001
From: "Vladimir D. Seleznev" <vseleznv@altlinux.org>
Date: Tue, 8 Oct 2019 18:29:40 +0300
Subject: [PATCH] ALT pkgs TEA

---
 tcl/pkgs/itcl4.2.1/Makefile.in           | 15 ++++++++-------
 tcl/pkgs/itcl4.2.1/configure             |  2 +-
 tcl/pkgs/sqlite3.34.0/Makefile.in        |  4 ++--
 tcl/pkgs/tdbc1.1.2/Makefile.in           | 19 ++++++++++---------
 tcl/pkgs/tdbc1.1.2/configure             |  2 +-
 tcl/pkgs/tdbcmysql1.1.2/Makefile.in      | 13 +++++++------
 tcl/pkgs/tdbcmysql1.1.2/tclconfig/tcl.m4 |  2 +-
 tcl/pkgs/tdbcodbc1.1.2/Makefile.in       | 13 +++++++------
 tcl/pkgs/tdbcpostgres1.1.2/Makefile.in   | 13 +++++++------
 tcl/pkgs/tdbcsqlite3-1.1.2/Makefile.in   | 13 +++++++------
 tcl/pkgs/thread2.8.6/Makefile.in         | 15 ++++++++-------
 11 files changed, 59 insertions(+), 52 deletions(-)

diff --git a/tcl/pkgs/itcl4.2.1/Makefile.in b/tcl/pkgs/itcl4.2.1/Makefile.in
index ef1cba0d1c..3fd49ccb8a 100644
--- a/tcl/pkgs/itcl4.2.1/Makefile.in
+++ b/tcl/pkgs/itcl4.2.1/Makefile.in
@@ -81,8 +81,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= @abs_top_builddir@
@@ -401,6 +401,7 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
+	@mkdir -p "$(DESTDIR)$(pkglibdir)" "$(DESTDIR)$(pkgdatadir)"
 	@$(INSTALL_DATA_DIR) "$(DESTDIR)$(pkglibdir)"
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
@@ -419,15 +420,15 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p "$(DESTDIR)$(pkglibdir)/$$destp"; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p "$(DESTDIR)$(pkgdatadir)/$$destp"; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl "$(DESTDIR)$(pkglibdir)"; \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl "$(DESTDIR)$(pkgdatadir)"; \
 	fi
-	$(INSTALL_DATA) itclConfig.sh "$(DESTDIR)$(pkglibdir)"
+	$(INSTALL_DATA) itclConfig.sh "$(DESTDIR)$(libdir)"
 
 #========================================================================
 # Install binary executables (e.g. .exe files and dependent .dll files)
diff --git a/tcl/pkgs/itcl4.2.1/configure b/tcl/pkgs/itcl4.2.1/configure
index 1cfaa1220b..28914a9933 100755
--- a/tcl/pkgs/itcl4.2.1/configure
+++ b/tcl/pkgs/itcl4.2.1/configure
@@ -8392,7 +8392,7 @@ $as_echo "${TCLSH_PROG}" >&6; }
     #--------------------------------------------------------------------
 
     # pkglibdir must be a fully qualified path and (not ${exec_prefix}/lib)
-    eval pkglibdir="${libdir}/itcl${PACKAGE_VERSION}"
+    eval pkglibdir="${libdir}/tcl/${PACKAGE_NAME}${PACKAGE_VERSION}"
     if test "${TCL_LIB_VERSIONS_OK}" = "ok"; then
 	eval itcl_LIB_FLAG="-litcl${PACKAGE_VERSION}"
 	eval itcl_STUB_LIB_FLAG="-litclstub${PACKAGE_VERSION}"
diff --git a/tcl/pkgs/sqlite3.34.0/Makefile.in b/tcl/pkgs/sqlite3.34.0/Makefile.in
index 6b73e35eaa..a1a744aa67 100644
--- a/tcl/pkgs/sqlite3.34.0/Makefile.in
+++ b/tcl/pkgs/sqlite3.34.0/Makefile.in
@@ -405,8 +405,8 @@ install-lib-binaries: binaries
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl "$(DESTDIR)$(pkglibdir)"; \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl "$(DESTDIR)$(pkgdatadir)"; \
 	fi
 
 #========================================================================
diff --git a/tcl/pkgs/tdbc1.1.2/Makefile.in b/tcl/pkgs/tdbc1.1.2/Makefile.in
index ee02c14985..9995001fac 100644
--- a/tcl/pkgs/tdbc1.1.2/Makefile.in
+++ b/tcl/pkgs/tdbc1.1.2/Makefile.in
@@ -79,8 +79,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= @abs_top_builddir@
@@ -426,6 +426,7 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
+	@mkdir -p $(DESTDIR)$(pkglibdir) $(DESTDIR)$(pkgdatadir)
 	@$(INSTALL_DATA_DIR) $(DESTDIR)$(pkglibdir)
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
@@ -435,7 +436,7 @@ install-lib-binaries: binaries
 	    if test "x$$ext" = "xdll"; then \
 		lib=`basename $$p|sed -e 's/.[^.]*$$//'`.lib; \
 		if test -f $$lib; then \
-		    echo " $(INSTALL_DATA) $$lib $(DESTDIR)$(pkglibdir)/$$lib"; \
+		    echo " $(INSTALL_DATA) $$lib $(DESTDIR)$(pkgdir)/$$lib"; \
 	            $(INSTALL_DATA) $$lib $(DESTDIR)$(pkglibdir)/$$lib; \
 		fi; \
 	    fi; \
@@ -444,16 +445,16 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkglibdir)/$$destp; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkgdatadir)/$$destp; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkglibdir); \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkgdatadir); \
 	fi
-	@echo " Install tdbcConfig.sh $(DESTDIR)$(pkglibdir)"; \
-	$(INSTALL_DATA) tdbcConfig.sh $(DESTDIR)$(pkglibdir)/tdbcConfig.sh
+	@echo " Install tdbcConfig.sh $(DESTDIR)$(libdir)"; \
+	$(INSTALL_DATA) tdbcConfig.sh $(DESTDIR)$(libdir)/tdbcConfig.sh
 
 #========================================================================
 # Install binary executables (e.g. .exe files and dependent .dll files)
diff --git a/tcl/pkgs/tdbc1.1.2/configure b/tcl/pkgs/tdbc1.1.2/configure
index 8711596b9a..5a5de51b3e 100755
--- a/tcl/pkgs/tdbc1.1.2/configure
+++ b/tcl/pkgs/tdbc1.1.2/configure
@@ -8476,7 +8476,7 @@ fi
     #--------------------------------------------------------------------
 
     # pkglibdir must be a fully qualified path and (not ${exec_prefix}/lib)
-    eval pkglibdir="${libdir}/tdbc${PACKAGE_VERSION}"
+    eval pkglibdir="${libdir}/tcl/${PACKAGE_NAME}${PACKAGE_VERSION}"
     if test "${TCL_LIB_VERSIONS_OK}" = "ok"; then
 	eval tdbc_LIB_FLAG="-ltdbc${PACKAGE_VERSION}"
 	eval tdbc_STUB_LIB_FLAG="-ltdbcstub${PACKAGE_VERSION}"
diff --git a/tcl/pkgs/tdbcmysql1.1.2/Makefile.in b/tcl/pkgs/tdbcmysql1.1.2/Makefile.in
index b552d88612..b8a4330918 100644
--- a/tcl/pkgs/tdbcmysql1.1.2/Makefile.in
+++ b/tcl/pkgs/tdbcmysql1.1.2/Makefile.in
@@ -79,8 +79,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= @abs_top_builddir@
@@ -422,6 +422,7 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
+	@mkdir -p $(DESTDIR)$(pkglibdir) $(DESTDIR)$(pkgdatadir)
 	@$(INSTALL_DATA_DIR) $(DESTDIR)$(pkglibdir)
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
@@ -440,13 +441,13 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkglibdir)/$$destp; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkgdatadir)/$$destp; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkglibdir); \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkgdatadir); \
 	fi
 
 #========================================================================
diff --git a/tcl/pkgs/tdbcmysql1.1.2/tclconfig/tcl.m4 b/tcl/pkgs/tdbcmysql1.1.2/tclconfig/tcl.m4
index 8d24e7ff35..e4dbc794a3 100644
--- a/tcl/pkgs/tdbcmysql1.1.2/tclconfig/tcl.m4
+++ b/tcl/pkgs/tdbcmysql1.1.2/tclconfig/tcl.m4
@@ -3877,7 +3877,7 @@ AC_DEFUN([TEA_EXPORT_CONFIG], [
     #--------------------------------------------------------------------
 
     # pkglibdir must be a fully qualified path and (not ${exec_prefix}/lib)
-    eval pkglibdir="[$]{libdir}/$1${PACKAGE_VERSION}"
+    eval pkglibdir="[$]{libdir}/tcl/${PACKAGE_NAME}${PACKAGE_VERSION}"
     if test "${TCL_LIB_VERSIONS_OK}" = "ok"; then
 	eval $1_LIB_FLAG="-l$1${PACKAGE_VERSION}"
 	eval $1_STUB_LIB_FLAG="-l$1stub${PACKAGE_VERSION}"
diff --git a/tcl/pkgs/tdbcodbc1.1.2/Makefile.in b/tcl/pkgs/tdbcodbc1.1.2/Makefile.in
index 85d07b5720..72a45aeb91 100644
--- a/tcl/pkgs/tdbcodbc1.1.2/Makefile.in
+++ b/tcl/pkgs/tdbcodbc1.1.2/Makefile.in
@@ -79,8 +79,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= @abs_top_builddir@
@@ -433,6 +433,7 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
+	@mkdir -p $(DESTDIR)$(pkglibdir) $(DESTDIR)$(pkgdatadir)
 	@$(INSTALL_DATA_DIR) $(DESTDIR)$(pkglibdir)
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
@@ -451,13 +452,13 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkglibdir)/$$destp; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkgdatadir)/$$destp; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkglibdir); \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkgdatadir); \
 	fi
 
 #========================================================================
diff --git a/tcl/pkgs/tdbcpostgres1.1.2/Makefile.in b/tcl/pkgs/tdbcpostgres1.1.2/Makefile.in
index 7ae8aa4262..df3e78df49 100644
--- a/tcl/pkgs/tdbcpostgres1.1.2/Makefile.in
+++ b/tcl/pkgs/tdbcpostgres1.1.2/Makefile.in
@@ -79,8 +79,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= @abs_top_builddir@
@@ -424,6 +424,7 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
+	@mkdir -p $(DESTDIR)$(pkglibdir) $(DESTDIR)$(pkgdatadir)
 	@$(INSTALL_DATA_DIR) $(DESTDIR)$(pkglibdir)
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
@@ -442,13 +443,13 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkglibdir)/$$destp; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkgdatadir)/$$destp; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkglibdir); \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkgdatadir); \
 	fi
 
 #========================================================================
diff --git a/tcl/pkgs/tdbcsqlite3-1.1.2/Makefile.in b/tcl/pkgs/tdbcsqlite3-1.1.2/Makefile.in
index a52af1e58b..e809da5576 100644
--- a/tcl/pkgs/tdbcsqlite3-1.1.2/Makefile.in
+++ b/tcl/pkgs/tdbcsqlite3-1.1.2/Makefile.in
@@ -79,8 +79,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)-$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= @abs_top_builddir@
@@ -403,6 +403,7 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
+	@mkdir -p $(DESTDIR)$(pkglibdir) $(DESTDIR)$(pkgdatadir)
 	@$(INSTALL_DATA_DIR) $(DESTDIR)$(pkglibdir)
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
@@ -421,13 +422,13 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkglibdir)/$$destp; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkgdatadir)/$$destp; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkglibdir); \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkgdatadir); \
 	fi
 
 #========================================================================
diff --git a/tcl/pkgs/thread2.8.6/Makefile.in b/tcl/pkgs/thread2.8.6/Makefile.in
index 9697a81f17..3beabe74de 100644
--- a/tcl/pkgs/thread2.8.6/Makefile.in
+++ b/tcl/pkgs/thread2.8.6/Makefile.in
@@ -79,8 +79,8 @@ mandir		= @mandir@
 DESTDIR		=
 
 PKG_DIR		= $(PACKAGE_NAME)$(PACKAGE_VERSION)
-pkgdatadir	= $(datadir)/$(PKG_DIR)
-pkglibdir	= $(libdir)/$(PKG_DIR)
+pkgdatadir	= $(libdir)/tcl/$(PKG_DIR)
+pkglibdir	= $(libdir)/tcl/$(PKG_DIR)
 pkgincludedir	= $(includedir)/$(PKG_DIR)
 
 top_builddir	= .
@@ -401,7 +401,8 @@ distclean: clean
 #========================================================================
 
 install-lib-binaries: binaries
-	@$(INSTALL_DATA_DIR) "$(DESTDIR)$(pkglibdir)"
+	@mkdir -p "$(DESTDIR)$(pkglibdir)" "$(DESTDIR)$(pkgdatadir)"
+	@$(INSTALL_DATA_DIR) "$(DESTDIR)$(pkgdatadir)"
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    echo " $(INSTALL_LIBRARY) $$p $(DESTDIR)$(pkglibdir)/$$p"; \
@@ -427,13 +428,13 @@ install-lib-binaries: binaries
 	@list='$(PKG_TCL_SOURCES)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    destp=`basename $$p`; \
-	    echo " Install $$destp $(DESTDIR)$(pkglibdir)/$$destp"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p "$(DESTDIR)$(pkglibdir)/$$destp"; \
+	    echo " Install $$destp $(DESTDIR)$(pkgdatadir)/$$destp"; \
+	    $(INSTALL_DATA) $(srcdir)/$$p "$(DESTDIR)$(pkgdatadir)/$$destp"; \
 	  fi; \
 	done
 	@if test "x$(SHARED_BUILD)" = "x1"; then \
-	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkglibdir)"; \
-	    $(INSTALL_DATA) pkgIndex.tcl "$(DESTDIR)$(pkglibdir)"; \
+	    echo " Install pkgIndex.tcl $(DESTDIR)$(pkgdatadir)"; \
+	    $(INSTALL_DATA) pkgIndex.tcl "$(DESTDIR)$(pkgdatadir)"; \
 	fi
 
 #========================================================================
-- 
2.29.2

